import { Routes } from '@angular/router';
import { EmployeeStatusListComponent } from './employee-status-list.component';


export const routes: Routes = [
    {
        path: '', component: EmployeeStatusListComponent
    }
];
